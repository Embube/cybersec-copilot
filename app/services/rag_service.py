def answer_question(question: str) -> dict:
    # Scaffold only. Replace with Chroma/FAISS + document ingestion when ready.
    return {
        "answer": "RAG scaffold is present, but vector indexing is not enabled in this package yet.",
        "sources": [],
    }
